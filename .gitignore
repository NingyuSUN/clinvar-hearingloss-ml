__pycache__/
*.pyc
*.egg-info/
.eggs/
build/
dist/
.venv/
.ipynb_checkpoints/
.pytest_cache/
.DS_Store

# re-run artifacts (the frozen reference set is tracked; per-fold dumps are not)
/results/per_fold_*.csv
/results/oof_*.csv
/results/analysis_summary.json
# keep the compressed matrix, not an uncompressed local copy
/data/modeling_matrix.csv

# predict_variants.py refuses to write into a non-empty directory, so demo
# runs and ad hoc predict output directories should never be committed
/artifacts/
/my_output/
